import { EnterDirective } from './enter.directive';

xdescribe('EnterDirective', () => {
  it('should create an instance', () => {
    // const directive = new EnterDirective();
    // expect(directive).toBeTruthy();
  });
});
